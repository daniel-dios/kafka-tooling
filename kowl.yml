
kafka:
  protobuf:
    enabled: true
    mappings:
      - topicName: transactions
        valueProtoType: de.n26.demokafkaui.userTransaction
    fileSystem:
      enabled: true
      refreshInterval: 5m
      paths:
        - /etc/protos
